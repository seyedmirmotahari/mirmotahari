const portfolioItems = [
    { title: "Point of View", category: "Design", href: "#", description: "Created for the Faculty of Fine Arts at the University of Lisbon, this series transforms the building into a cinematic space, where each student’s journey unfolds like a film—unique, evolving, and shaped by their own creative path.", shortDescription: "Cinematic journeys, unique paths", images: ["design/pointofview1.jpg", "design/pointofview2.jpg", "design/pointofview3.jpg"], cardImage: "design/pointofview.jpg" },
    {
        title: "Mumbai Guidebook",
        category: "Design",
        href: "#",
        shortDescription: "Interactive narrated book.",
        description: "The Mumbai Guidebook, created in 2012, is an interactive narrated book showcasing 24 of the city’s most captivating places for tourists. The user target was Iranian or anyone who could read Persian. Our team handled the photography, data collection, and overall design.",
        images: ["design/MumbaiGuidebook1.jpg", "design/MumbaiGuidebook2.jpg"],
        cardImage: "design/MumbaiGuidebook.jpg"
    },
                                { title: "Maze", category: "Design", href: "#", description: "The game was designed for branding purposes for Rougine Darou, a pharmaceutical company. We incorporated the company’s slogan into the wooden maze, creating an interactive physical experience that reinforced the brand.", shortDescription: "Branding game", images: [
                                    "design/Maze.jpg",
                                    "design/Maze1.jpg"
                                ], cardImage: "design/Maze.jpg" },
                            { title: "Raboo", category: "Design", href: "#", description: "The 48-page catalog was designed for Raboo, the sanitary manufacturer. Our team handled the layout, photography of over 80 products, and the complete printing process.", shortDescription: "Catalog Design", images: [
                                "design/Rabo1.jpg",
                                "design/Rabo2.jpg",
                                "design/Rabo3.jpg",
                                "design/Rabo4.jpg",
                                "design/Rabo5.jpg",
                                "design/Rabo6.jpg",
                                "design/Rabo7.jpg",
                                // ...existing code...
                            ], cardImage: "design/Rabo.jpg" },
                    // ...existing code...
                    { title: "Gift of the Day", category: "Design", href: "#", description: "The app was designed in 2013 with the aim of creating an interactive calendar and reminder application, as well as a tool for managing meetings and appointments. It was developed for Iranian users, who at the time did not have access to an application supporting the Shamsi calendar for daily planning.", shortDescription: "Interactive Calendar", images: [
                        "design/InteractiveCalender.jpg"
                    ], cardImage: "design/InteractiveCalender.jpg" },
                { title: "Infographic", category: "Design", href: "#", description: "The infographic map was designed for the Industrial Design Centre (IDC), Indian Institute of Technology Bombay (IITB).", shortDescription: "IDC Map", images: ["design/IDC-Map1.jpg"], cardImage: "design/IDC-Map.jpg" },
            { title: "Calendar", category: "Design", href: "#", description: "The Calendar project included both a notebook calendar and a desk calendar. For the notebook calendar, our team designed the cover and the interior layout. We were also responsible for overseeing and managing the entire printing process. For the desk calendar, each month featured a photograph of employees whose birthdays fell within that month. Our team handled the photography, the design, the calendar layout, as well as the complete printing and production process.", shortDescription: "Desk and diary calendar", images: [
                "design/DeskCalendar&Diary.jpg",
                "design/DeskCalendar&Diary1.jpg",
                "design/DeskCalendar&Diary2.jpg"
            ] },
            { title: "K&D", category: "Design", href: "#", description: "The 48-page catalog was designed for Knowledge and Design (K&D), the sanitary manufacturer. Our team handled the layout, photography of over 80 products, and the complete printing process.", shortDescription: "Catalog Design", images: [
                "design/K&D1.jpg",
                "design/K&D2.jpg",
                "design/K&D3.jpg",
                "design/K&D4.jpg",
                "design/K&D5.jpg",
                "design/K&D6.jpg",
                "design/K&D7.jpg"
            ], cardImage: "design/K&D.jpg" },
        { title: "Industrial Photography", category: "Photography", href: "#", description: "Over 250 industrial product photographs for manufacturers Raboo, K&D, and TheOne.", shortDescription: "Raboo, K&D, and TheOne", images: [
            "photography/IndustrialPhotography.jpg",
            "photography/IndustrialPhotography1.jpg",
            "photography/IndustrialPhotography2.jpg",
            "photography/IndustrialPhotography3.jpg",
            "photography/IndustrialPhotography4.jpg",
            "photography/IndustrialPhotography5.jpg",
            "photography/IndustrialPhotography6.jpg",
            "photography/IndustrialPhotography7.jpg",
            "photography/IndustrialPhotography8.jpg",
            "photography/IndustrialPhotography9.jpg",
            // ...existing code...
        ], cardImage: "photography/IndustrialPhotography.jpg" },
    { title: "Watch ME!", category: "Design", href: "#", description: "Watch Me! is an Iranian short animated film directed by Reza Mehranfar. The animation follows the story of a young boy who lives in a world filled with anxiety and fear — feelings that are invisible and seemingly meaningless to others around him, leaving him in emotional darkness and isolation. Through its narrative, the film highlights the gap between children’s inner experiences and the adult world that often fails to understand or acknowledge them. Watch Me! was selected to be screened at the International Film Festival on Disability in France, where it stood out as the only work from Iran included in the festival’s program, which focuses on themes related to health and disability awareness.", shortDescription: "Poster, DVD, End Credits Design", images: ["design/WatchMe1.jpg", "design/WatchMe2.jpg", "design/WatchMe3.jpg"], cardImage: "design/WatchMe.jpg" },
    { title: "Pernigotti", category: "Menu design", href: "Mumbai'sGuidebook.html", description: "Menu design (Including photography)", shortDescription: "Menu design", images: ["design/Pernigotti1.jpg", "design/Pernigotti2.jpg", "design/Pernigotti3.jpg", "design/Pernigotti4.jpg", "design/Pernigotti5.jpg", "design/Pernigotti6.jpg", "design/Pernigotti7.jpg", "design/Pernigotti8.jpg"], cardImage: "design/Pernigotti.jpg", randomImage: false },
    { title: "Social media posts", category: "Design", href: "InteractiveCalender.html", description: "Over 82 posts have been designed for Rougine Darou, a pharmaceutical company, covering information about disease and more.", shortDescription: "Social media posts", images: [
        "design/Rougine1.mp4",
        "design/Rougine2.mp4",
        "design/Rougine3.mp4",
        "design/Rougine4.mp4",
        "design/Rougine5.mp4",
        "design/Rougine6.mp4",
        "design/Rougine7.mp4",
        "design/Rougine8.mp4",
        "design/Rougine9.mp4",
        "design/Rougine10.mp4",
        "design/Rougine11.mp4",
        "design/Rougine12.mp4",
        "design/Rougine13.mp4",
        "design/Rougine14.mp4",
        "design/Rougine15.mp4"
    ], cardImage: "design/RougineSocial.jpg" },
        { title: "A Gift with Purpose", category: "Design", href: "#", description: "The gift was created as part of a meaningful campaign for Doctors’ Day and Pharmacists’ Day for the pharmaceutical company Rugine Darou, which supplies medications for serious diseases such as cancer. Instead of spending a large budget on a conventional gift, we developed a purpose-driven concept that supports children under the care of doctors and pharmacists. As part of the campaign, over 300 children participated by creating paintings, which we then collected and carefully framed to present as heartfelt gifts to the doctors and pharmacists. The entire campaign process from concept development and artwork collection to framing, packaging, and final production was fully handled by our team.", shortDescription: "Meaningful Giving Campaign", images: ["design/RougineGift.jpg"], cardImage: "design/RougineGift.jpg" },
    { title: "Flash memory", category: "Graphic Design", href: "IDC.html", description: "The flash memory was designed for the pharmaceutical company Rugine Darou, incorporating the company’s logo for branding purposes, with the entire printing and production process managed by the team.", shortDescription: "Logo branding", images: ["design/FlashMemory.jpg"] },
    { title: "lowimpact.design", category: "Design", href: "#", description: "LowImpact.design focuses on practical steps digital designers can take throughout the design process to make their work more energy-efficient and environmentally friendly.<br>Link to access: <a href=\"https://lowimpactdesign.me\" target=\"_blank\">lowimpactdesign.me</a>", shortDescription: "Sustainable design practices", images: ["design/LowImpactDesign.jpg"] },
    { title: "Collection of Crap", category: "Illustration", href: "BioData.html", description: "This collection is a personal painting project created using mixed painting techniques in a notebook format. Collage and acrylic are used intuitively, allowing the process to be guided by freedom and experimentation. Every page tells a story, presenting different perspectives and personal expressions.", shortDescription: "Freedom Experimented", images: ["illustration/CollectionofCrap1.jpg", "illustration/CollectionofCrap2.jpg", "illustration/CollectionofCrap3.jpg", "illustration/CollectionofCrap4.jpg", "illustration/CollectionofCrap5.jpg", "illustration/CollectionofCrap6.jpg", "illustration/CollectionofCrap7.jpg", "illustration/CollectionofCrap8.jpg", "illustration/CollectionofCrap9.jpg", "illustration/CollectionofCrap10.jpg", "illustration/CollectionofCrap11.jpg", "illustration/CollectionofCrap12.jpg", "illustration/CollectionofCrap13.jpg"] },
        { title: "Barazandeh Paris", category: "Photography", href: "Barazandeh.Paris.html", description: "The photography for Barazandeh Paris, a French luxury brand, features over 80 product shots and 20+ modeling images of gloves and Swarovski-decorated hosiery, highlighting hand-stitched craftsmanship and the playful yet decadent elegance of the collection.", shortDescription: "Product and modeling photography", images: ["photography/Barazandeh.jpg", "photography/Barazandeh1.jpg", "photography/Barazandeh2.jpg"] },
// Removed 'Sanitary Valves' portfolio item
    { title: "BILT Cellotape Campaign", category: "Design", href: "#", description: "A comprehensive advertising campaign featuring over 10 unique visual concepts for BILT. Our team led the entire creative process, from original character design and commercial photography to executing large-scale outdoor (OOH) and indoor media placements.", shortDescription: "Over 10 advertisements", images: ["design/Bilt1.jpg"] },
    { title: "Magazine", category: "Design", href: "#", description: "Over 15 magazines have been designed (both print and digital) for Pardis Gholhak Cinema, based in Tehran, Iran. The focus is on Western, American, French, Noir, and other film genres.", shortDescription: "Film magazines, print & digital", images: ["design/Magazine1.jpg", "design/Magazine2.jpg", "design/Magazine3.jpg", "design/Magazine4.jpg"], cardImage: "design/Magazine.jpg" },
    { title: "Logo", category: "Design", href: "#", description: "Over 12 logos have been designed for different companies.", shortDescription: "Designed for multiple companies", images: [
        "design/Logo1.jpg", "design/Logo2.jpg", "design/Logo3.jpg", "design/Logo4.jpg", "design/Logo5.jpg", "design/Logo6.jpg", "design/Logo7.jpg", "design/Logo8.jpg", "design/Logo9.jpg", "design/Logo10.jpg",
        "design/Logo11.jpg", "design/Logo12.jpg"
    ] },
    { title: "The Black Monsters", category: "Illustration", href: "#", description: "The Black Monster Family is a personal project that explores creativity through bold black brush strokes on cardboard.", shortDescription: "Creativity Born from Darkness", images: ["illustration/Monster1.jpg", "illustration/Monster2.jpg", "illustration/Monster3.jpg", "illustration/Monster4.jpg", "illustration/Monster5.jpg", "illustration/Monster6.jpg", "illustration/Monster7.jpg", "illustration/Monster8.jpg", "illustration/Monster9.jpg", "illustration/Monster10.jpg", "illustration/Monster11.jpg", "illustration/Monster12.jpg", "illustration/Monster13.jpg", "illustration/Monster14.jpg", "illustration/Monster15.jpg", "illustration/Monster16.jpg"] },
    { title: "Story of India", category: "Illustration", href: "#", description: "Seven illustrated bookmarks is a personal project showing experiences from six years living and studying in India (2007–2013). It uses collages made from papers like ICICI Bank documents, university fee receipts, flight tickets, and more. The bookmarks were given to the people who helped during that time", shortDescription: "Bookmark", images: ["illustration/StpryOfIndia1.jpg"], cardImage: "illustration/StpryOfIndia.jpg" },
        { title: "Diterin", category: "Design", href: "#", description: "The Diterin catalog design and printing process was handled by our team.", shortDescription: "Catalog design", images: ["design/Diterin1.jpg", "design/Diterin2.jpg"], cardImage: "design/Diterin.jpg" },
];

const ITEMS_PER_LOAD = 6;

const STAR_COLORS = [
    "#ff6347",
    "#1e90ff",
    "#3cb371",
    "#ffa500",
    "#8a2be2",
    "#ff69b4",
    "#40e0d0",
    "#ffd700",
    "#ba55d3",
    "#00bfff"
];

function safeStorageGet(key) {
    try {
        return window.localStorage ? localStorage.getItem(key) : null;
    } catch (e) {
        return null;
    }
}

function safeStorageSet(key, value) {
    try {
        if (window.localStorage) {
            localStorage.setItem(key, value);
        }
    } catch (e) {
        // Ignore storage errors so core UI still renders
    }
}

const MEDIA_FOLDER_CASE_MAP = {
    design: "design",
    illustration: "illustration",
    photography: "photography",
    photo: "photo",
    main: "main"
};

function normalizeMediaPath(assetPath) {
    if (typeof assetPath !== "string" || assetPath.trim() === "") {
        return assetPath;
    }

    const querySplitIndex = assetPath.search(/[?#]/);
    const rawPath = querySplitIndex === -1 ? assetPath : assetPath.slice(0, querySplitIndex);
    const suffix = querySplitIndex === -1 ? "" : assetPath.slice(querySplitIndex);

    const segments = rawPath.split("/").filter(Boolean);
    if (segments.length < 2) {
        return assetPath;
    }

    const folder = MEDIA_FOLDER_CASE_MAP[segments[0].toLowerCase()] || segments[0];
    const filePath = segments.slice(1).join("/");
    return `${folder}/${filePath}${suffix}`;
}

function normalizeMediaList(list) {
    if (!Array.isArray(list)) {
        return [];
    }
    return list
        .map((asset) => normalizeMediaPath(asset))
        .filter((asset) => typeof asset === "string" && asset.trim() !== "");
}

function buildMediaPathCandidates(assetPath) {
    if (typeof assetPath !== "string" || assetPath.trim() === "") {
        return [];
    }

    const normalized = normalizeMediaPath(assetPath);
    const querySplitIndex = normalized.search(/[?#]/);
    const rawPath = querySplitIndex === -1 ? normalized : normalized.slice(0, querySplitIndex);
    const suffix = querySplitIndex === -1 ? "" : normalized.slice(querySplitIndex);
    const segments = rawPath.split("/").filter(Boolean);

    if (segments.length < 2) {
        return [normalized];
    }

    const folder = MEDIA_FOLDER_CASE_MAP[segments[0].toLowerCase()] || segments[0];
    const filePath = segments.slice(1).join("/");
    const pathSet = new Set([`${folder}/${filePath}${suffix}`]);

    const lastSlashIndex = filePath.lastIndexOf("/");
    const fileDir = lastSlashIndex === -1 ? "" : `${filePath.slice(0, lastSlashIndex + 1)}`;
    const fileNameWithExt = lastSlashIndex === -1 ? filePath : filePath.slice(lastSlashIndex + 1);
    const extDotIndex = fileNameWithExt.lastIndexOf(".");

    if (extDotIndex === -1) {
        pathSet.add(`${folder}/${fileDir}${fileNameWithExt.toLowerCase()}${suffix}`);
        pathSet.add(`${folder}/${fileDir}${fileNameWithExt.toUpperCase()}${suffix}`);
        return Array.from(pathSet);
    }

    const baseName = fileNameWithExt.slice(0, extDotIndex);
    const extension = fileNameWithExt.slice(extDotIndex);
    const baseVariants = new Set([
        baseName,
        baseName.toLowerCase(),
        baseName.toUpperCase(),
        `${baseName.charAt(0).toUpperCase()}${baseName.slice(1)}`,
        `${baseName.charAt(0).toLowerCase()}${baseName.slice(1)}`
    ]);
    const extVariants = new Set([extension, extension.toLowerCase(), extension.toUpperCase()]);

    baseVariants.forEach((name) => {
        extVariants.forEach((ext) => {
            pathSet.add(`${folder}/${fileDir}${name}${ext}${suffix}`);
        });
    });

    return Array.from(pathSet);
}

function createResilientImageElement(primarySrc, {
    alt = "",
    objectFit = "cover"
} = {}) {
    const img = document.createElement("img");
    img.alt = alt;
    img.style.width = "100%";
    img.style.height = "100%";
    img.style.objectFit = objectFit;

    const candidates = buildMediaPathCandidates(primarySrc);
    let candidateIndex = 0;

    const tryNextSource = () => {
        if (candidateIndex >= candidates.length) {
            img.removeEventListener("error", tryNextSource);
            return;
        }
        img.src = candidates[candidateIndex];
        candidateIndex += 1;
    };

    img.addEventListener("error", tryNextSource);
    tryNextSource();

    return img;
}

function createStarLayer({
    container,
    count,
    mode,
    isHeader = false
}) {
    const layer = document.createElement("div");
    layer.className = `star-layer star-layer--${mode}${isHeader ? " star-layer--header" : ""}`;

    for (let i = 0; i < count; i += 1) {
        const star = document.createElement("span");
        const shouldBlink = Math.random() < 0.45;
        star.className = shouldBlink ? "star star--blink" : "star";

        let size = Math.random() * 1.8 + 0.8;
        // Make day-mode (light) stars larger for better visibility
        if (mode === "light") {
            size = size * 1.6;
        }
        const left = Math.random() * 100;
        const top = Math.random() * 100;
        const duration = Math.random() * 5 + 2.5;
        const delay = Math.random() * -6;
        const opacity = Math.random() * 0.6 + 0.2;

        star.style.width = `${size}px`;
        star.style.height = `${size}px`;
        star.style.left = `${left}%`;
        star.style.top = `${top}%`;
        star.style.opacity = opacity.toFixed(2);
        if (shouldBlink) {
            star.style.animationDuration = `${duration.toFixed(2)}s`;
            star.style.animationDelay = `${delay.toFixed(2)}s`;
        }

        if (mode === "dark") {
            star.style.backgroundColor = "rgba(255, 255, 255, 0.9)";
        } else {
            const color = STAR_COLORS[Math.floor(Math.random() * STAR_COLORS.length)];
            star.style.backgroundColor = color;
        }

        layer.appendChild(star);
    }

    // Configure layer positioning:
    layer.style.pointerEvents = 'none';
    if (isHeader) {
        // Header layers should sit on top of the big image inside the header box
        layer.style.position = 'absolute';
        layer.style.left = '0';
        layer.style.top = '0';
        layer.style.width = '100%';
        layer.style.height = '100%';
        layer.style.zIndex = '1';
        layer.dataset.isHeader = 'true';
        container.appendChild(layer);
    } else {
        // Global layers stay fixed in the viewport background
        layer.style.position = 'fixed';
        layer.style.left = '0';
        layer.style.top = '0';
        layer.style.width = '100%';
        layer.style.height = '100%';
        // ensure stars sit above the header box but below UI (nav)
        layer.style.zIndex = '2';
        document.body.appendChild(layer);
    }
}

function updateHeaderLayerPositions() {
    const headerLayers = document.querySelectorAll('.star-layer[data-is-header]');
    headerLayers.forEach((layer) => {
        const target = layer.__headerTarget;
        if (!target) return;
        const rect = target.getBoundingClientRect();
        layer.style.left = `${rect.left}px`;
        layer.style.top = `${rect.top}px`;
        layer.style.width = `${rect.width}px`;
        layer.style.height = `${rect.height}px`;
    });
}

function updateStarTransforms(scrollY) {
    // No-op: star layers should not move on scroll. Ensure any transforms are cleared.
    const layers = document.querySelectorAll('.star-layer');
    layers.forEach((layer) => {
        layer.style.transform = 'none';
    });
}

function initStarFields() {
    if (document.querySelector(".star-layer")) {
        return;
    }

    // Calculate star counts based on viewport; increase global density so stars appear
    // across the page (not heavily concentrated only in the header).
    const globalCountBase = Math.round((window.innerWidth * window.innerHeight) / 9000);
    const globalCount = Math.min(400, Math.max(180, globalCountBase));
    const headerCount = Math.min(140, Math.max(70, Math.round(window.innerWidth / 8)));
    // Ensure global layer has at least twice the header density so stars are everywhere
    const globalCountAdjusted = Math.max(globalCount, headerCount * 2);
    const darkGlobalCount = Math.max(1, Math.round(globalCountAdjusted * 0.8));
    const darkHeaderCount = Math.max(1, Math.round(headerCount * 0.8));

    createStarLayer({ container: document.body, count: globalCountAdjusted, mode: "light" });
    createStarLayer({ container: document.body, count: darkGlobalCount, mode: "dark" });

    // Do not create separate header-specific star layers; use global layers
    // so stars remain in the background everywhere (including over the header image).

    // Initialize header positions; star layers remain static (no scroll movement)
    updateHeaderLayerPositions();
    updateStarTransforms(0);
    window.addEventListener('resize', () => {
        updateHeaderLayerPositions();
        updateStarTransforms(0);
    });
}

function initThemeToggle() {
    const toggleButton = document.querySelector("#mode-toggle");
    if (!toggleButton) {
        return;
    }

    const setMode = (isDark) => {
        document.body.classList.toggle("dark-mode", isDark);
        toggleButton.setAttribute("aria-pressed", String(isDark));
        toggleButton.textContent = isDark ? "Day mode" : "Night mode";
        safeStorageSet("theme", isDark ? "dark" : "light");
    };

    const savedTheme = safeStorageGet("theme");
    setMode(savedTheme === "dark");

    toggleButton.addEventListener("click", () => {
        const isDark = !document.body.classList.contains("dark-mode");
        setMode(isDark);
    });
}

function initMusicToggle() {
    const btn = document.querySelector("#music-toggle");
    if (!btn) return;

    let audio = document.querySelector("#site-music");
    if (!audio) {
        audio = document.createElement("audio");
        audio.id = "site-music";
        audio.loop = true;
        audio.preload = "auto";
        audio.setAttribute("playsinline", "");
        audio.src = normalizeMediaPath("main/music.mp3");
        audio.setAttribute("aria-hidden", "true");
        document.body.appendChild(audio);
    }

    const gestureEvents = ["pointerdown", "touchstart", "click", "keydown"];
    const addGestureListeners = (handler) => {
        gestureEvents.forEach((evt) => {
            window.addEventListener(evt, handler, { once: true });
        });
    };
    const removeGestureListeners = (handler) => {
        gestureEvents.forEach((evt) => {
            window.removeEventListener(evt, handler);
        });
    };

    const fadeInAudio = (targetVolume = 1, duration = 1400) => {
        try {
            audio.volume = 0;
            audio.muted = false;
        } catch (e) {}
        const steps = Math.max(1, Math.round(duration / 50));
        const increment = targetVolume / steps;
        let currentVolume = 0;
        const intervalId = setInterval(() => {
            currentVolume = Math.min(targetVolume, currentVolume + increment);
            audio.volume = currentVolume;
            if (currentVolume >= targetVolume - 0.001) {
                audio.volume = targetVolume;
                clearInterval(intervalId);
            }
        }, 50);
    };

    const setMusic = (on) => {
        btn.setAttribute("aria-pressed", String(on));
        btn.textContent = "Music";
        safeStorageSet("music", on ? "on" : "off");
        if (on) {
            audio.muted = false;
            audio.play().then(() => {
                fadeInAudio(1, 1400);
            }).catch(() => {
                // Autoplay may be blocked; ignore error
            });
        } else {
            try {
                audio.pause();
                audio.currentTime = 0;
            } catch (e) {
                // ignore
            }
        }
    };


    const saved = safeStorageGet("music");
    if (saved === "on") {
        // Try to play immediately; if blocked, try muted autoplay, then wait for user interaction to unmute/play
        audio.play().then(() => {
            setMusic(true);
        }).catch(() => {
            audio.muted = true;
            audio.play().then(() => {
                setMusic(true);
                // Unmute on first user interaction
                const unmute = () => {
                    audio.muted = false;
                    removeGestureListeners(unmute);
                };
                addGestureListeners(unmute);
            }).catch(() => {
                // As a final fallback, wait for user interaction to start playback
                const tryPlay = () => {
                    audio.play().then(() => {
                        setMusic(true);
                        removeGestureListeners(tryPlay);
                    }).catch(() => {});
                };
                addGestureListeners(tryPlay);
            });
        });
    } else if (saved === "off") {
        setMusic(false);
    } else {
        // No saved preference — attempt autoplay, otherwise wait for user interaction
        audio.play().then(() => {
            setMusic(true);
        }).catch(() => {
            setMusic(false);
            const tryPlay = () => {
                audio.play().then(() => {
                    setMusic(true);
                    removeGestureListeners(tryPlay);
                }).catch(() => {});
            };
            addGestureListeners(tryPlay);
        });
    }

    btn.addEventListener("click", () => {
        const isOn = btn.getAttribute("aria-pressed") === "true";
        setMusic(!isOn);
    });
}

function createPortfolioCard(item) {
    const imageList = normalizeMediaList(Array.isArray(item.images) ? item.images : (item.image ? [item.image] : []));
    const article = document.createElement("article");
    article.className = "portfolio-item";

    const link = document.createElement("a");
    link.className = "item-link";
    link.href = item.href;

    const image = document.createElement("div");
    image.className = "item-image";
    if (imageList.length > 0) {
        let imageSrc;
        if (item.cardImage) {
            imageSrc = normalizeMediaPath(item.cardImage);
        } else {
            const randomIndex = item.randomImage === false ? 0 : Math.floor(Math.random() * imageList.length);
            imageSrc = imageList[randomIndex];
        }
        const img = createResilientImageElement(imageSrc, { alt: item.title, objectFit: "cover" });
        img.loading = "lazy";
        image.appendChild(img);
    }

    const title = document.createElement("h2");
    title.className = "item-title";
    title.textContent = item.title;

    const category = document.createElement("span");
    category.className = "item-category";
    category.textContent = item.shortDescription || item.description || item.category;

    link.appendChild(image);
    link.appendChild(title);
    link.appendChild(category);

    article.appendChild(link);

    return article;
}

function initPortfolio() {
        const siteMusic = document.getElementById('site-music');
    const grid = document.querySelector(".portfolio-grid");
    const loadMoreButton = document.querySelector("#load-more");
    const filterLinks = document.querySelectorAll(".nav-link[data-filter]");
    const modal = document.querySelector("#portfolio-modal");
    const profileTrigger = document.querySelector("[data-profile-modal]");
    const modalTitle = document.querySelector("#modal-title");
    const modalCategory = document.querySelector("#modal-category");
    const modalImage = document.querySelector("#modal-image");
    const modalPrev = document.querySelector("[data-modal-prev]");
    const modalNext = document.querySelector("[data-modal-next]");
    const modalCloseButtons = document.querySelectorAll("[data-modal-close]");

    if (!grid || !loadMoreButton) {
        return;
    }

    const hasModal = Boolean(modal && modalTitle && modalCategory && modalImage && modalPrev && modalNext);

    let visibleCount = 0;
    let mohsenVoiceAudio = null;
    let mohsenMusicAudio = null;
    let mohsenPrevSiteMusicState = null;

    const fadeSiteMusicIn = (targetVolume = 1, duration = 1400) => {
        if (!siteMusic) return;
        try {
            siteMusic.muted = false;
            siteMusic.volume = 0;
        } catch (e) {}

        const steps = Math.max(1, Math.round(duration / 50));
        const increment = targetVolume / steps;
        let currentVolume = 0;

        const intervalId = setInterval(() => {
            currentVolume = Math.min(targetVolume, currentVolume + increment);
            try { siteMusic.volume = currentVolume; } catch (e) {}

            if (currentVolume >= targetVolume - 0.001) {
                try { siteMusic.volume = targetVolume; } catch (e) {}
                clearInterval(intervalId);
            }
        }, 50);
    };

    const stopMohsenSequence = () => {
        if (mohsenVoiceAudio) {
            try { mohsenVoiceAudio.pause(); } catch (e) {}
            try { mohsenVoiceAudio.currentTime = 0; } catch (e) {}
            try { mohsenVoiceAudio.src = ''; } catch (e) {}
            mohsenVoiceAudio = null;
        }
        if (mohsenMusicAudio) {
            try { mohsenMusicAudio.pause(); } catch (e) {}
            try { mohsenMusicAudio.currentTime = 0; } catch (e) {}
            try { mohsenMusicAudio.src = ''; } catch (e) {}
            mohsenMusicAudio = null;
        }
        if (siteMusic && mohsenPrevSiteMusicState) {
            const wasPaused = mohsenPrevSiteMusicState.paused;
            const targetVolume = mohsenPrevSiteMusicState.volume;
            const wasMuted = mohsenPrevSiteMusicState.muted;

            if (!wasPaused) {
                siteMusic.muted = false;
                siteMusic.play().catch(() => {});
                fadeSiteMusicIn(targetVolume, 1400);
            } else {
                try {
                    siteMusic.muted = wasMuted;
                    siteMusic.volume = targetVolume;
                } catch (e) {}
                siteMusic.pause();
            }
            mohsenPrevSiteMusicState = null;
        }
    };
    let activeFilter = "all";
    let filteredItems = portfolioItems;

    let activeImages = [];
    let activeIndex = 0;

    const modalImageWrap = document.querySelector('.modal-image-wrap');
    const updateModalImage = () => {
        // Pause site music if social media post popup is open
        if (modalTitle.textContent === "Social media posts" && siteMusic && !siteMusic.paused) {
            siteMusic.pause();
        }
        if (!hasModal) {
            return;
        }
        // Remove any previous data-popup attribute
        if (modalImageWrap) {
            modalImageWrap.removeAttribute('data-popup');
        }
        // Set a unique data-popup attribute based on the title (spaces to dashes, lowercase)
        if (modalTitle.textContent) {
            const popupKey = modalTitle.textContent.trim().toLowerCase().replace(/\s+/g, '-');
            if (modalImageWrap) {
                modalImageWrap.setAttribute('data-popup', popupKey);
            }
        }
        // Add special class for social media post (optional, for legacy CSS)
        if (modalTitle.textContent === "Social media posts") {
            if (modalImageWrap) {
                modalImageWrap.classList.add("modal-image-wrap--social");
            }
        } else {
            if (modalImageWrap) {
                modalImageWrap.classList.remove("modal-image-wrap--social");
            }
        }

        // Add a class to the modal itself for very tall popups (e.g., Watch ME!) so we can limit height on small screens.
        if (modalTitle.textContent && modalTitle.textContent.trim().toLowerCase() === "watch me!") {
            modal.classList.add("watch-me-popup");
        } else {
            modal.classList.remove("watch-me-popup");
        }
        if (activeImages.length > 0) {
            const currentImage = activeImages[activeIndex];
            if (currentImage.endsWith('.mp4') || currentImage.endsWith('.webm') || currentImage.endsWith('.ogg')) {
                // It's a video
                const objectFit = (modalTitle.textContent === "Social media posts") ? 'contain' : 'cover';
                const isSocial = (modalTitle.textContent === "Social media posts");
                const isMohsen = currentImage.toLowerCase().includes('mohsen.mp4') || modalTitle.textContent === "Mohsen Mirmotahari";
                const mutedAttr = isSocial ? '' : 'muted';
                // social posts: autoplay+loop; mohsen: autoplay once then freeze at last frame; others: show controls
                const videoAttrs = isSocial ? 'autoplay loop playsinline' : (isMohsen ? 'autoplay playsinline' : 'playsinline controls');
                modalImage.innerHTML = `<video ${videoAttrs} ${mutedAttr} style="width: 100%; height: 100%; object-fit: ${objectFit};">
                    <source src="${currentImage}" type="video/mp4">
                    Your browser does not support the video tag.
                </video>`;
                modalImage.style.backgroundImage = "none";
                // Attach handlers for special cases (e.g., freeze mohsen at last frame)
                const insertedVideo = modalImage.querySelector('video');
                if (insertedVideo) {
                    if (isMohsen) {
                        insertedVideo.addEventListener('ended', () => {
                            try {
                                // Seek to just before the end to reliably show the last frame
                                const lastTime = Math.max(0, insertedVideo.duration - 0.05);
                                insertedVideo.currentTime = lastTime;
                            } catch (e) {}
                            try { insertedVideo.pause(); } catch (e) {}
                        });
                        // Try to play; if autoplay is blocked, show controls so user can play manually
                        insertedVideo.play().catch(() => {
                            insertedVideo.setAttribute('controls', 'controls');
                        });
                    } else if (!isSocial) {
                        // For non-social, non-mohsen videos ensure controls are usable if autoplay blocked
                        insertedVideo.play().catch(() => {
                            insertedVideo.setAttribute('controls', 'controls');
                        });
                    }
                }
            } else {
                // It's an image
                modalImage.innerHTML = "";
                const modalImg = createResilientImageElement(currentImage, { objectFit: "cover" });
                modalImg.style.transition = "opacity 0.5s ease";
                modalImage.appendChild(modalImg);
                modalImage.style.backgroundImage = "none";
            }
        } else {
            modalImage.style.backgroundImage = "none";
            modalImage.innerHTML = "";
        }

        if (activeImages.length <= 1) {
            modalPrev.classList.add("is-hidden");
            modalNext.classList.add("is-hidden");
        } else {
            if (activeIndex > 0) {
                modalPrev.classList.remove("is-hidden");
            } else {
                modalPrev.classList.add("is-hidden");
            }
            if (activeIndex < activeImages.length - 1) {
                modalNext.classList.remove("is-hidden");
            } else {
                modalNext.classList.add("is-hidden");
            }
        }
    };

    const normalizeCategory = (value) => String(value || "").toLowerCase().trim();

    const matchesFilter = (item) => {
        if (activeFilter === "all") {
            return true;
        }

        const category = normalizeCategory(item.category);

        if (activeFilter === "photography") {
            return category === "photography";
        }
        if (activeFilter === "illustration") {
            return category === "illustration";
        }
        if (activeFilter === "design") {
            return category.includes("design");
        }
        return true;
    };

    const renderNextItems = () => {
        const nextItems = filteredItems.slice(visibleCount, visibleCount + ITEMS_PER_LOAD);
        nextItems.forEach((item) => {
            const card = createPortfolioCard(item);
            if (hasModal) {
                card.querySelector(".item-link").addEventListener("click", (event) => {
                    event.preventDefault();
                    activeImages = normalizeMediaList(Array.isArray(item.images) ? item.images : (item.image ? [item.image] : []));
                    activeIndex = 0;
                    modalTitle.textContent = item.title;
                    modalCategory.innerHTML = item.description || item.category;
                    updateModalImage();
                    modal.classList.add("is-open");
                    modal.setAttribute("aria-hidden", "false");
                    document.body.classList.add("modal-open");
                });
            }
            grid.appendChild(card);
        });
        visibleCount += nextItems.length;

        if (visibleCount >= filteredItems.length) {
            loadMoreButton.style.display = "none";
        } else {
            loadMoreButton.style.display = "inline-block";
        }
    };

    const setActiveNav = (filter) => {
        filterLinks.forEach((link) => {
            link.classList.toggle('active', link.dataset.filter === filter);
        });
        if (contactLink) {
            contactLink.classList.toggle('active', filter === 'contact');
        }
    };

    const applyFilter = (filter) => {
        activeFilter = filter;
        filteredItems = portfolioItems.filter(matchesFilter);
        visibleCount = 0;
        grid.innerHTML = "";
        setActiveNav(filter);
        renderNextItems();
    };

    filterLinks.forEach((link) => {
        link.addEventListener("click", (event) => {
            event.preventDefault();
            applyFilter(link.dataset.filter || "all");
            window.scrollTo({ top: 0, behavior: 'smooth' });
        });
    });

    // Prevent default for contact link
    const contactLink = document.querySelector('.nav-link[href="#contact"]');
    if (contactLink) {
        contactLink.addEventListener("click", (event) => {
            event.preventDefault();
            setActiveNav('contact');
            // Removed scrollIntoView to prevent scroll
            // const contactSection = document.getElementById('contact');
            // if (contactSection) {
            //     contactSection.scrollIntoView({ behavior: 'smooth' });
            // }
        });
    }

    loadMoreButton.addEventListener("click", renderNextItems);

    if (hasModal) {
        modalPrev.addEventListener("click", () => {
            if (activeImages.length <= 1 || activeIndex <= 0) {
                return;
            }
            const newIndex = activeIndex - 1;
            const currentImg = modalImage.querySelector('img');
            if (currentImg) {
                const newImg = document.createElement('img');
                newImg.src = activeImages[newIndex];
                newImg.style.cssText = "width: 100%; height: 100%; object-fit: cover; transition: opacity 0.5s ease; position: absolute; top: 0; left: 0; opacity: 0;";
                newImg.alt = "";
                modalImage.appendChild(newImg);
                newImg.offsetHeight; // Force reflow
                newImg.style.opacity = '1';
                setTimeout(() => {
                    activeIndex = newIndex;
                    modalImage.innerHTML = `<img src="${activeImages[activeIndex]}" style="width: 100%; height: 100%; object-fit: cover; transition: opacity 0.5s ease;" alt="">`;
                    updateModalImage();
                }, 500);
            } else {
                activeIndex = newIndex;
                updateModalImage();
            }
        });

        modalNext.addEventListener("click", () => {
            if (activeImages.length <= 1 || activeIndex >= activeImages.length - 1) {
                return;
            }
            const newIndex = activeIndex + 1;
            const currentImg = modalImage.querySelector('img');
            if (currentImg) {
                const newImg = document.createElement('img');
                newImg.src = activeImages[newIndex];
                newImg.style.cssText = "width: 100%; height: 100%; object-fit: cover; transition: opacity 0.5s ease; position: absolute; top: 0; left: 0; opacity: 0;";
                newImg.alt = "";
                modalImage.appendChild(newImg);
                newImg.offsetHeight; // Force reflow
                newImg.style.opacity = '1';
                setTimeout(() => {
                    activeIndex = newIndex;
                    modalImage.innerHTML = `<img src="${activeImages[activeIndex]}" style="width: 100%; height: 100%; object-fit: cover; transition: opacity 0.5s ease;" alt="">`;
                    updateModalImage();
                }, 500);
            } else {
                activeIndex = newIndex;
                updateModalImage();
            }
        });

        // Close modal helper used by buttons and keyboard (with fade-out)
        const closeModal = () => {
            if (!modal.classList.contains("is-open") || modal.classList.contains("is-closing")) return;

            // Immediately stop Mohsen voice/music sequence and restore local site music state before fade-out
            stopMohsenSequence();

            modal.classList.add("is-closing");
            modal.setAttribute("aria-hidden", "true");
            document.body.classList.remove("modal-open");

            const finishClose = () => {
                modal.classList.remove("is-open", "is-closing");
                // restore modal state in case it was a text-only popup
                try { modal.classList.remove('text-only-popup'); } catch(e) {}
                try { modalImageWrap.style.display = ''; } catch(e) {}
                try { modalPrev.classList.remove('is-hidden'); modalNext.classList.remove('is-hidden'); } catch(e) {}
                // restore footer dino if this was opened by the dino
                try { if (modal.classList.contains('dino-open')) { const f = document.getElementById('dino-footer-img'); if (f) f.style.visibility = ''; modal.classList.remove('dino-open'); } } catch(e) {}
                // Stop mohsen audio sequence and restore music state
                stopMohsenSequence();
                // Stop video playback by clearing modalImage content
                modalImage.innerHTML = "";
                // Resume site music if it was paused for social media post
                if (modalTitle.textContent === "Social media posts" && siteMusic && siteMusic.paused) {
                    siteMusic.play().catch(() => {});
                }
            };

            const onTransitionEnd = (event) => {
                if (event.target !== modal || event.propertyName !== "opacity") return;
                finishClose();
                modal.removeEventListener("transitionend", onTransitionEnd);
            };
            modal.addEventListener("transitionend", onTransitionEnd);

            // Fallback if transitionend doesn't fire (match CSS timing)
            setTimeout(() => {
                if (modal.classList.contains("is-closing")) {
                    finishClose();
                    modal.removeEventListener("transitionend", onTransitionEnd);
                }
            }, 620);
        };

        modalCloseButtons.forEach((button) => {
            button.addEventListener("click", closeModal);
        });

        // Close modal on ESC
        document.addEventListener("keydown", (event) => {
            if (event.key === "Escape" && modal.classList.contains("is-open")) {
                closeModal();
            }
        });

        const fadeAudioIn = (audio, targetVolume, duration = 1400) => {
            const steps = Math.max(1, Math.round(duration / 50));
            const increment = targetVolume / steps;
            let currentVolume = 0;
            audio.volume = 0;
            const stepInterval = setInterval(() => {
                currentVolume = Math.min(targetVolume, currentVolume + increment);
                audio.volume = currentVolume;
                if (currentVolume >= targetVolume - 0.001) {
                    audio.volume = targetVolume;
                    clearInterval(stepInterval);
                }
            }, 50);
        };

        const playMohsenSequence = () => {
            stopMohsenSequence();

            if (siteMusic) {
                mohsenPrevSiteMusicState = {
                    paused: siteMusic.paused,
                    muted: siteMusic.muted,
                    volume: siteMusic.volume
                };
                // Mute (but keep playing) during voice playback
                siteMusic.muted = true;
            }

            mohsenVoiceAudio = new Audio(normalizeMediaPath("main/1.wav"));
            mohsenMusicAudio = new Audio(normalizeMediaPath("main/2.wav"));
            mohsenVoiceAudio.preload = "auto";
            mohsenMusicAudio.preload = "auto";

            mohsenVoiceAudio.addEventListener('ended', () => {
                // Play second audio after voice complete, keep site music muted until second audio ends
                mohsenMusicAudio.play().catch((err) => {
                    console.warn('Second audio playback failed:', err);
                });
            });

            mohsenMusicAudio.addEventListener('ended', () => {
                if (siteMusic && mohsenPrevSiteMusicState) {
                    siteMusic.muted = false;
                    const targetVolume = mohsenPrevSiteMusicState.volume;
                    if (!mohsenPrevSiteMusicState.paused) {
                        siteMusic.volume = 0;
                        siteMusic.play().catch(() => {});
                        fadeAudioIn(siteMusic, targetVolume, 1400);
                    } else {
                        siteMusic.volume = targetVolume;
                        if (mohsenPrevSiteMusicState.muted) {
                            siteMusic.muted = true;
                        }
                        siteMusic.pause();
                    }
                    mohsenPrevSiteMusicState = null;
                }
                stopMohsenSequence();
            });

            mohsenVoiceAudio.play().catch((err) => {
                console.warn('First audio playback failed:', err);
            });
        };

        if (profileTrigger) {
            profileTrigger.addEventListener("click", (event) => {
                event.preventDefault();
                activeImages = normalizeMediaList(["photo/mohsen.mp4"]);
                activeIndex = 0;
                modalTitle.textContent = "Mohsen Mirmotahari";
                modalCategory.innerHTML = "<p>Mohsen was educated in Iran, India, Malaysia, and Portugal. He is currently a PhD student in Communication Design at the Faculty of Fine Arts, University of Lisbon. His research investigates sustainable digital design practices and promotes them among digital designers. His academic background includes a master’s degree in design (Visual Communication) from the Indian Institute of Technology Bombay (IIT Bombay), a bachelor’s degree in fine arts (Visual Communication Design) from the University of Pune, India, and research experience at the University of Malaya, Malaysia. </p><p style=\"text-indent: 20px; margin-top: 10px;\">With over two decades of professional experience, Mohsen has worked as a designer across print and digital publications and has participated in more than fifteen exhibitions worldwide. He founded <i>Mirmotahari Design Studio</i> in Iran and collaborates with companies in France, Germany, and Portugal. He is also the founder of <a href=\"#\" class=\"open-lowimpact\"><i>lowimpact.design</i></a>, a project that both follows and presents sustainable digital design practices.</p>";
                updateModalImage();
                modal.classList.remove("is-closing");
                modal.classList.add("is-open");
                modal.setAttribute("aria-hidden", "false");
                document.body.classList.add("modal-open");

                // Play 1.wav then 2.wav for the profile modal
                playMohsenSequence();
            });
        }

        // Dino footer click: open a modal showing the dino image above the text (no title)
        (function(){
            const dinoImg = document.getElementById('dino-footer-img');
            if (!dinoImg) return;
            dinoImg.addEventListener('click', function(e){
                e.preventDefault();
                if (!modal || !modalCategory || !modalImage) return;
                modalTitle.textContent = '';
                // put a small Dino2 image above the text inside the category area so the modal can size to content
                try {
                    if (modalImageWrap) modalImageWrap.style.display = 'none';
                    if (modalImage) { modalImage.innerHTML = ''; modalImage.style.backgroundImage = 'none'; }
                    if (modalCategory) {
                        modalCategory.innerHTML = `<img src="${normalizeMediaPath("main/Dino2.png")}" alt="Dino" class="dino-modal-img">` +
                            '<div class="dino-popup-text">Every second we use digital design, we consume energy and increase our carbon footprint. If we don\'t act responsibly, we risk resource shortages and rising pollution. It\'s time to embrace sustainable digital design.</div>';
                    }
                } catch(e) {}
                // hide navigation controls
                try { modalPrev.classList.add('is-hidden'); modalNext.classList.add('is-hidden'); } catch(e) {}
                // mark modal as text-only so CSS can shrink it
                modal.classList.add('text-only-popup');
                // mark this modal as opened from dino so we can hide the footer dino
                try { modal.classList.add('dino-open'); } catch(e) {}
                // hide the footer dino while modal is open
                try { const f = document.getElementById('dino-footer-img'); if (f) f.style.visibility = 'hidden'; } catch(e) {}
                modal.classList.remove('is-closing');
                modal.classList.add('is-open');
                modal.setAttribute('aria-hidden', 'false');
                document.body.classList.add('modal-open');
            });
        })();

        // Helper to open a portfolio item by its title (used by internal links)
        const openItemByTitle = (title) => {
            // Ensure Mohsen voice/music stops when navigating from his profile to another portfolio popup.
            stopMohsenSequence();

            const item = portfolioItems.find(i => i.title && i.title.toLowerCase() === title.toLowerCase());
            if (!item) return;
            activeImages = normalizeMediaList(Array.isArray(item.images) ? item.images : (item.image ? [item.image] : []));
            activeIndex = 0;
            modalTitle.textContent = item.title;
            modalCategory.innerHTML = item.description || item.category;
            updateModalImage();
            modal.classList.remove("is-closing");
            modal.classList.add("is-open");
            modal.setAttribute("aria-hidden", "false");
            document.body.classList.add("modal-open");
        };

        // Delegate clicks on the modalCategory area to handle internal portfolio links
        modalCategory.addEventListener('click', (e) => {
            const trigger = e.target.closest && e.target.closest('.open-lowimpact');
            if (trigger) {
                e.preventDefault();
                openItemByTitle('lowimpact.design');
            }
        });
    }

    applyFilter('design');
}

if ("scrollRestoration" in history) {
    history.scrollRestoration = "manual";
}

document.addEventListener("DOMContentLoaded", () => {
    if (window.location.hash) {
        history.replaceState(null, document.title, window.location.pathname + window.location.search);
    }
    try { initStarFields(); } catch (e) { console.warn("initStarFields failed", e); }
    try { initThemeToggle(); } catch (e) { console.warn("initThemeToggle failed", e); }
    try { initMusicToggle(); } catch (e) { console.warn("initMusicToggle failed", e); }
    try { initPortfolio(); } catch (e) { console.error("initPortfolio failed", e); }

});

// System status and weather updates
document.addEventListener('DOMContentLoaded', function() {
    // Update time every minute
    function updateTime() {
        const now = new Date();
        const lisbonTime = now.toLocaleTimeString('en-GB', { 
            timeZone: 'Europe/Lisbon', 
            hour: '2-digit', 
            minute: '2-digit' 
        });
        const timeEl = document.getElementById('server-time');
        if (timeEl) timeEl.textContent = lisbonTime;
    }
    updateTime();
    setInterval(updateTime, 60000);

    // Fetch system info
    let isFetchingSysinfo = false;
    async function fetchSysinfo() {
        if (isFetchingSysinfo) {
            return;
        }
        isFetchingSysinfo = true;
        try {
            const res = await fetch('/sysinfo', { cache: 'no-store' });
            if (!res.ok) throw new Error('Sysinfo fetch failed');
            const data = await res.json();
            const hasValue = (value) => value !== null && value !== undefined;
            
            // Update system usage
            const cpuEl = document.getElementById('system-cpu');
            const cpuTempEl = document.getElementById('system-cpu-temp');
            const ramEl = document.getElementById('system-ram');
            const memEl = document.getElementById('system-mem');
            const powerEl = document.getElementById('server-power-used');
            const uptimeEl = document.getElementById('server-uptime');
            
            if (cpuEl) cpuEl.textContent = hasValue(data.cpu_percent) ? `${Number(data.cpu_percent).toFixed(1)}%` : '-';
            if (cpuTempEl) cpuTempEl.textContent = hasValue(data.cpu_temp) ? `${Number(data.cpu_temp).toFixed(1)}°C` : '-';
            if (ramEl) ramEl.textContent = hasValue(data.ram_percent) ? `${Number(data.ram_percent).toFixed(1)}%` : '-';
            if (memEl) memEl.textContent = hasValue(data.disk_usage) ? `${data.disk_usage}%` : '-';
            if (powerEl) powerEl.textContent = hasValue(data.power_used) ? `${data.power_used}W` : '-';
            if (uptimeEl) uptimeEl.textContent = hasValue(data.uptime) && String(data.uptime).trim() !== '' ? data.uptime : '-';
            
            // Update server stats
            const panelEl = document.getElementById('server-panel-status');
            const batteryLevelEl = document.getElementById('server-battery-level');
            const batteryStatusEl = document.getElementById('server-battery-status');
            const batteryTempEl = document.getElementById('server-battery-temp');
            
            if (panelEl) panelEl.textContent = hasValue(data.panel_voltage) ? `${data.panel_voltage}V` : '-';
            if (batteryLevelEl) batteryLevelEl.textContent = hasValue(data.battery_voltage) ? `${data.battery_voltage}V` : '-%';
            if (batteryStatusEl) batteryStatusEl.textContent = data.battery_status || 'No Signal';
            if (batteryTempEl) batteryTempEl.textContent = hasValue(data.battery_temp) ? `${data.battery_temp}°C` : '-';
        } catch (err) {
            console.error('Failed to fetch sysinfo:', err);
        } finally {
            isFetchingSysinfo = false;
        }
    }
    fetchSysinfo();
    setInterval(() => {
        if (!document.hidden) {
            fetchSysinfo();
        }
    }, 5000); // Update every 5 seconds while page is visible

    // Fetch weather forecast
    async function fetchWeather() {
        try {
            const lat = 38.7223; // Lisbon latitude
            const lon = -9.1393; // Lisbon longitude
            const url = `https://api.open-meteo.com/v1/forecast?latitude=${lat}&longitude=${lon}&current_weather=true&daily=weathercode,temperature_2m_max,temperature_2m_min&timezone=Europe/Lisbon&forecast_days=3`;
            const res = await fetch(url);
            if (!res.ok) throw new Error('Weather fetch failed');
            const data = await res.json();

            if (!data || !data.daily || !Array.isArray(data.daily.weathercode) || !Array.isArray(data.daily.temperature_2m_max)) {
                throw new Error('Unexpected weather response shape');
            }
            
            const weatherCodes = data.daily.weathercode;
            const maxTemps = data.daily.temperature_2m_max;
            const currentTemp = data.current_weather ? data.current_weather.temperature : null;
            
            function getWeatherDescription(code) {
                const descriptions = {
                    0: 'Sunny',
                    1: 'Sunny',
                    2: 'Cloudy',
                    3: 'Cloudy',
                    45: 'Foggy',
                    48: 'Foggy',
                    51: 'Rainy',
                    53: 'Rainy',
                    55: 'Rainy',
                    56: 'Rainy',
                    57: 'Rainy',
                    61: 'Rainy',
                    63: 'Rainy',
                    65: 'Rainy',
                    66: 'Rainy',
                    67: 'Rainy',
                    71: 'Snowy',
                    73: 'Snowy',
                    75: 'Snowy',
                    77: 'Snowy',
                    80: 'Shower',
                    81: 'Shower',
                    82: 'Shower',
                    85: 'Snow shower',
                    86: 'Snow shower',
                    95: 'Stormy',
                    96: 'Stormy',
                    99: 'Stormy'
                };
                return descriptions[code] || 'Unknown';
            }
            
            const todayEl = document.getElementById('forecast-today');
            const tomorrowEl = document.getElementById('forecast-tomorrow');
            const dayAfterEl = document.getElementById('forecast-day-after');
            if (weatherCodes.length < 3 || maxTemps.length < 3) {
                throw new Error('Incomplete weather forecast data');
            }
            
            if (todayEl) {
                const todayDesc = getWeatherDescription(weatherCodes[0]);
                const todayTemp = currentTemp !== null
                    ? `${Math.round(currentTemp)}°`
                    : `${Math.round(maxTemps[0])}°`;
                todayEl.textContent = `${todayDesc}, ${todayTemp}`;
            }
            if (tomorrowEl) {
                const tomorrowDesc = getWeatherDescription(weatherCodes[1]);
                const tomorrowTemp = `${Math.round(maxTemps[1])}°`;
                tomorrowEl.textContent = `${tomorrowDesc}, ${tomorrowTemp}`;
            }
            if (dayAfterEl) {
                const dayAfterDesc = getWeatherDescription(weatherCodes[2]);
                const dayAfterTemp = `${Math.round(maxTemps[2])}°`;
                dayAfterEl.textContent = `${dayAfterDesc}, ${dayAfterTemp}`;
            }
        } catch (err) {
            console.error('Failed to fetch weather:', err);
        }
    }
    fetchWeather();
    // Update weather every 4 hours
    setInterval(fetchWeather, 4 * 60 * 60 * 1000);
});

// Contact overlay
var contactLink = document.querySelector('.nav-link[href="#contact"]');
var footerLinks = document.querySelector('.footer-links');
if (contactLink && footerLinks) {
    var previousActiveFilter = null;
    // Create overlay
    var overlay = document.querySelector('.contact-overlay-dark');
    if (!overlay) {
      overlay = document.createElement('div');
      overlay.className = 'contact-overlay-dark hide';
      overlay.style.display = 'none';
      document.body.appendChild(overlay);
    }
    // Create popup container for cloned links
    var popupLinks = document.createElement('div');
    popupLinks.className = 'footer-links contact-highlight';
    popupLinks.style.display = 'none';
    document.body.appendChild(popupLinks);
    function showContactOverlay() {
            var activeFilterLink = document.querySelector('.nav-link[data-filter].active');
            previousActiveFilter = activeFilterLink ? activeFilterLink.dataset.filter : null;
      overlay.classList.remove('hide');
      overlay.style.display = 'block';
      // Clone footerLinks content for popup
      popupLinks.innerHTML = footerLinks.innerHTML;
      popupLinks.style.display = 'block';
      overlay.addEventListener('click', hideContactOverlay);
      document.addEventListener('keydown', escHandler);
      // document.getElementById('contact').scrollIntoView({behavior:'smooth'}); // Removed to prevent scroll
      document.body.style.overflow = 'hidden'; // Prevent background scroll
    }
    function hideContactOverlay() {
      overlay.classList.add('hide');
      setTimeout(function(){ overlay.style.display = 'none'; }, 400);
      popupLinks.style.display = 'none';
      overlay.removeEventListener('click', hideContactOverlay);
      document.removeEventListener('keydown', escHandler);
      document.body.style.overflow = '';

            // Restore nav state: remove contact highlight and re-activate previous filter.
            contactLink.classList.remove('active');
            var filterLinks = document.querySelectorAll('.nav-link[data-filter]');
            filterLinks.forEach(function(link) { link.classList.remove('active'); });

            var restoreTarget = null;
            if (previousActiveFilter) {
                restoreTarget = document.querySelector('.nav-link[data-filter="' + previousActiveFilter + '"]');
            }
            if (!restoreTarget) {
                restoreTarget = document.querySelector('.nav-link[data-filter="design"]') || filterLinks[0];
            }
            if (restoreTarget) {
                restoreTarget.classList.add('active');
            }
    }
    function escHandler(e) {
      if (e.key === 'Escape') hideContactOverlay();
    }
    contactLink.addEventListener('click', function(e) {
      e.preventDefault();
      showContactOverlay();
      window.scrollTo({ top: 0, behavior: 'smooth' });
    });
}
